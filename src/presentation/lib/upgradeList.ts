/**
 * Legacy upgrade list entry point. Upgrades panel is Vue (useUpgradeList + UpgradeCard).
 * These no-ops exist so code or mocks that reference this module still resolve.
 */
export function renderUpgradeList(_container?: HTMLElement): void {}

export function updateUpgradeListInPlace(): void {}
